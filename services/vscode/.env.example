# code-server environment.
#
# Copy to services/vscode/.env and set PASSWORD only if you want auth.
#
# Auth mode behavior:
# - PASSWORD empty  -> code-server runs with --auth none
# - PASSWORD set    -> code-server runs with --auth password
#
# Example:
# PASSWORD=change_me_strong_password
PASSWORD=
